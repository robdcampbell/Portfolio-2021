import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App className="global__styles" />
  </React.StrictMode>,
  document.getElementById("root")
);
